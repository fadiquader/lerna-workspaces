{
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "lerna": "lerna",
    "bootstrap": "lerna bootstrap",
    "dev": "lerna run build --stream --parallel"
  },
  "devDependencies": {
    "lerna": "^3.4.3",
    "rimraf": "2.6.2"
  }
}
